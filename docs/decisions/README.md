# 决策记录

本目录保存长期有效的架构决策记录。

当项目需要在多个有意义的方案之间做选择时，应单独创建一份决策记录。记录应简短、带日期，并能让后续接入的 Agent 理解当时为什么这样选择。

建议文件名：

```text
YYYY-MM-DD-short-decision-title.md
```

当前基线决策：

- 第一版优先采用原生 macOS 应用，以获得更好的动画流畅度、低开销和浮动窗口体验。
- TallyClaw 对上游工具和网关保持严格只读。
- TallyClaw 拥有自己的 SQLite 账本；一旦记录完成，永久总量不依赖源文件继续存在。

